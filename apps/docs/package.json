{
	"name": "modfolio-docs",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "echo \"docs not scaffolded yet\" && exit 1",
		"build": "echo \"docs not scaffolded yet\" && exit 1",
		"lint": "echo \"no lint configured yet\"",
		"lint:fix": "echo \"no lint configured yet\""
	}
}
